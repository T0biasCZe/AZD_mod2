package com.tobikcze.azd_mod;

public class ClientProxy extends CommonProxy {

    // Override CommonProxy methods here, if you want a different behaviour on the client (e.g. registering renders).
    // Don't forget to call the super methods as well.
    /*
     * @Override
     * public void preInit(FMLPreInitializationEvent event){
     * GameRegistry.registerTileEntity(TileEntityThreeDBlock.class, "tileThreeDBlock");
     * ClientRegistry.bindTileEntitySpecialRenderer(TileEntityThreeDBlock.class, new RenderTileEntityThreeDBlock());
     * }
     */
}
